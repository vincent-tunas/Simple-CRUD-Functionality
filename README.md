# Project Description

A simple CRUD-Functionality web application that accepts user inputs from the front-end to be stored into a mock backend JSON server. This data that are already made and stored can be created, read, Update and deleted.

![Alt text](user-interface.png)

